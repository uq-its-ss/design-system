import { Meta, Story, Canvas } from '@storybook/blocks';
import StatusBadge from "../../../.storybook/custom/components/StatusBadge";
import * as BreadcrumbStories from './breadcrumb.stories';

<Meta of={BreadcrumbStories} />

# Breadcrumb

<StatusBadge.Stable />

<StatusBadge.StableDoc />

<a href="https://github.com/uq-its-ss/design-system/issues" class="uq-button uq-button--text" target="_blank">
  Check for open issues
</a>

## Overview

Breadcrumbs are a horizontal list of links used to indicate the current page location within the information architecture (the first level always being the UQ homepage). Page links are separated by a chevron icon which indicates the level of that page relative to the page links beside it. Breadcrumbs are truncated and ellipsed (...) when the text is longer than a set width, however the current page crumb is not linked and always listed in full. UQ homepage is the base.

<Canvas sourceState="hidden" withToolbar>
  {/* <Story id="components-breadcrumb--breadcrumb" /> is deprecated, please migrate it to <Story of={referenceToStory} /> see: https://storybook.js.org/migration-guides/7.0 */}

  <Story id="components-breadcrumb--breadcrumb" />
</Canvas>

```html
<nav class="uq-breadcrumb" aria-label="Breadcrumb">
  <ol class="uq-breadcrumb__list">
    <li class="uq-breadcrumb__item">
      <a class="uq-breadcrumb__link" title="Home" href="/">Home</a>
    </li>
    <li class="uq-breadcrumb__item">
      <a
        class="uq-breadcrumb__link"
        title="Level one"
        href="/information-and-services"
        >Level one</a
      >
    </li>
    <li class="uq-breadcrumb__item">
      <a
        class="uq-breadcrumb__link"
        title="Level two"
        href="/information-and-services/internet-and-wifi"
        >Level two</a
      >
    </li>
    <li class="uq-breadcrumb__item">
      <a
        class="uq-breadcrumb__link"
        title="Bachelor of Business with a Major in Management or Finance"
        href="#"
        >Bachelor of Business with a Major in Management or Finance</a
      >
    </li>
    <li class="uq-breadcrumb__item">
      <a
        class="uq-breadcrumb__link"
        title="Major Leadership and Management Science"
        href="#"
        >Major Leadership and Management Science</a
      >
    </li>
    <li class="uq-breadcrumb__item">Current page</li>
  </ol>
</nav>
```

### Truncation

When exceeding 4 breadcrumbs - the current page, parent, parent's parent and home crumbs will be shown with the additional crumbs collapsed behind an expand link. The full breadcrumb trail can then be viewed by clicking the expand crumb. *Note: only expand functionality is included, and cannot be collapsed again without a page refresh.*

### Wrapper

Wrap the `nav.uq-breadcrumb` element in a `div.uq-breadcrumb__container` element when including in the page layout. This provides correct padding, container width and the bottom border.

```html
<div class="uq-breadcrumb__container">
  <nav class="uq-breadcrumb" aria-label="Breadcrumb">
    <li class="uq-breadcrumb__item">
      <a class="uq-breadcrumb__link" title="Home" href="/">Home</a>
    </li>
  </nav>
</div>
```

## How and when to use

*   The breadcrumb reflects the information architecture of a page, not a historical navigation trail.
*   The component displays on a page once a user has navigated away from the home page to provide a navigation path back to the home and parent level pages.
*   The breadcrumb lists the current page however this should not be linked.

## Accessibility

For accessibility add `aria-label="Breadcrumb"` to the `nav` element that wraps the breadcrumb component.
