import { Meta, Canvas, Controls } from "@storybook/addon-docs/blocks";
import StatusBadge from "../../../.storybook/custom/components/StatusBadge";
import * as CardStories from "./card.stories";

<Meta of={CardStories} />

# Card

<StatusBadge.Stable /> <StatusBadge.StableDoc /> [Check for open
issues](https://github.com/uq-its-ss/design-system/issues)

## Overview

The card component is used to highlight a link to content.

<Canvas of={CardStories.Card} />

<Controls of={CardStories.Card} />

### Text

Use the text only version for smaller features.

<Canvas of={CardStories.Text} />

### Image

The standard card.

<Canvas of={CardStories.Image} />

### Icon

The icon card can include any [UQ icon](?path=/docs/components-icon--docs).

<Canvas of={CardStories.Icon} />

### Icon nav

The icon nav variant hides the description on mobile.

<Canvas of={CardStories.IconNav} />
