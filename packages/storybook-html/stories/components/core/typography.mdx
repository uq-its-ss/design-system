import { Meta, Story, Canvas, Preview } from "@storybook/blocks";
import * as TypographyExample from "./typography-example.stories";

<Meta title="Core/Typography" />

# Typography

**Roboto** and **Montserrat** are the primary fonts used across UQ's online applications.

The following table provides examples and style values of common HTML elements. NOTE: Individual components may override these values.

### Element examples

<Canvas of={TypographyExample.Example} />

### Loading fonts

Use the HTML or CSS below to load the Roboto and Montserrat fonts.

HTML

```html
<link
  rel="stylesheet"
  type="text/css"
  href="https://fonts.googleapis.com/css2?family=Montserrat:wght@500&family=Roboto:wght@300;400;500&display=swap"
/>
```

CSS

```css
@import url("https://fonts.googleapis.com/css2?family=Montserrat:wght@500&family=Roboto:wght@300;400;500&display=swap");
```

### Font family stacks

The following font family declarations use widely available fonts. A user's web browser will look at the comma-separated list and will
attempt to use each font in turn until it finds one that is available on the user's computer. The final "generic" font (sans-serif or serif) hints at what type of font to use if the web browser doesn't
find any of the fonts in the list.

**Roboto**

```css
font-family: "Roboto", "Helvetica Neue", Helvetica, Arial, sans-serif;
```

**Montserrat**

```css
font-family: Montserrat, "Helvetica Neue", Helvetica, Arial, sans-serif;
```

### Fallback fonts

When platforms and systems such as third party applications are unable to use the Roboto or Montserrat fonts, use the following fallback fonts. If none of the alternative font options are available, it is recommended to use the default font provided by the platform or system.

<p>
  <span class="uqds-font uqds-font--arial">Arial</span>: For headings and
  paragraph text
</p>

<p>
  <span class="uqds-font uqds-font--georgia uqds-font--italic">Georgia</span>:
  For blockquotes and testimonials
</p>
