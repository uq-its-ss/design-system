import { Story, Canvas } from "@storybook/addon-docs";
import StatusBadge from "../../../.storybook/custom/components/StatusBadge";
import { Section } from "./section.stories";

# Section

<StatusBadge.Development />
<StatusBadge.DraftDoc />

## Overview

The `.uq-section` component is a simple wrapper to use around other components.

It provides a uniform heading and description and can be used around card grids,
panes and image panels.

### How-to

Use the `.uq-section` element to provide a h2 heading and
description to introduce another set of components.

Sections should be placed inside a `.uq-sections` element to provide uniform
section spacing between each section.

Implement the markup structure below for the header.

<Canvas withSource="none">
  <Story id="components-section--section" />
</Canvas>

```html
<div class="uq-section">
  <div class="uq-section__header">
    <h2 class="uq-section__title">Title</h2>
    <div class="uq-section__description">
      <p>Description</p>
    </div>
  </div>
  <!-- Section content markup -->
</div>
```

### Shaded

A section with a grey background.

Modifier class: `.uq-section--shaded`

<Canvas withSource="none">
  <Story id="components-section--shaded" />
</Canvas>

### With container

Sections can also implement a `.uq-container` element within their markup so the
section can run edge-to-edge.

<Canvas withSource="none">
  <Story id="components-section--container" />
</Canvas>

### Shaded with container

For shaded sections that go edge-to-edge.

Modifier class: `.uq-section--shaded`

<Canvas withSource="none">
  <Story id="components-section--shaded-container" />
</Canvas>

### Examples

#### Section with Card grid

<Canvas withSource="none">
  <div class="uq-section">
    <div class="uq-container">
      <div class="uq-section__header">
        <h2 class="uq-section__title">Title</h2>
        <div class="uq-section__description">
          <p>Description</p>
        </div>
      </div>
      <div class="uq-card-grid uq-card-grid--target-3x">
        <div class="uqds-layout-demo-card"></div>
        <div class="uqds-layout-demo-card"></div>
        <div class="uqds-layout-demo-card"></div>
        <div class="uqds-layout-demo-card"></div>
        <div class="uqds-layout-demo-card"></div>
        <div class="uqds-layout-demo-card"></div>
      </div>
    </div>
  </div>
</Canvas>
