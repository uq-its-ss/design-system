import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import StatusBadge from "../../../.storybook/custom/components/StatusBadge";
import * as SectionStories from "./section.stories";

<Meta of={SectionStories} />

# Section

<StatusBadge.Development />
<StatusBadge.DraftDoc />

## Overview

The `.uq-section` component is a wrapper to use around other components.

It provides correct **section spacing** between sections by using
a responsive `96px` and `48px` `margin-bottom` for desktop and tablet/mobile respectively.

Additionally, it provides a uniform heading and description and can be used
around card grids and panes.

### How-to

Use the `.uq-section__header` element to provide a h2 heading and
description to introduce another set of components.

Implement the markup structure below for the header.

<Canvas of={SectionStories.Section} />

```html
<div class="uq-section">
  <div class="uq-section__header">
    <h2 class="uq-section__title">Title</h2>
    <div class="uq-section__description">
      <p>Description</p>
    </div>
  </div>
  <!-- Section content markup -->
</div>
```

### Shaded

A section with a grey background.

Modifier class: `.uq-section--shaded`

<Canvas of={SectionStories.Shaded} />

### With container

Sections can also implement a `.uq-container` element within their markup so the
section can run edge-to-edge.

<Canvas of={SectionStories.Container} />

### Shaded with container

For shaded sections that go edge-to-edge.

Modifier class: `.uq-section--shaded`

<Canvas of={SectionStories.ShadedContainer} />

### No margin

In cases where bottom margin is not desired.

Modifier class: `.uq-section--no-margin`

<Canvas of={SectionStories.NoMargin} />
